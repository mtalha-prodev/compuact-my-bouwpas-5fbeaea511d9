import { IconProp } from '@fortawesome/fontawesome-svg-core';
export interface IIconTagProps {
  trophy: {
    icon: IconProp;
  };
  check: {
    icon: IconProp;
  };
  sparkles: {
    icon: IconProp;
  };
  'triangle-exclamation': {
    icon: IconProp;
  };
  times: {
    icon: IconProp;
  };
  'link-slash': {
    icon: IconProp;
  };
  spinner: {
    icon: IconProp;
  };
  trash: {
    icon: IconProp;
  };
  'transporter-empty': {
    icon: IconProp;
  };
}
